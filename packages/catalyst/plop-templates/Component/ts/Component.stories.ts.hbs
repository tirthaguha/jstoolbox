import React from 'react';
import {{pascalCase name}} from './';
import defaultConfig from './{{pascalCase name}}.mock';

const BaseStory = {
  title: '{{type}}/{{pascalCase name}}',
  component: {{pascalCase name}}
};

export default BaseStory;
export const Story = () => <{{pascalCase name}} {...defaultConfig} />;