import React from 'react'; 
import './{{pascalCase name}}.{{styleExt}}';

interface {{pascalCase name}}Props {
  label: string;
};

const {{pascalCase name}}:React.FC<{{pascalCase name}}Props> = ({label}) => <div className="{{camelCase name}}">{label}</div>;

export default {{pascalCase name}};