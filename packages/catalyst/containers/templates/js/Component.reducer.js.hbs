
const initialState = {
  // Add your initial state here
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    case '{{constantCase name}}_ACTION':
      return { ...state};
    default:
      return state;
  }
};

