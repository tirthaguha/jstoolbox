import createContextProvider, { type IProvider, type TState, type IAction, getContextByName } from './ContextProvider';

export type { IProvider, TState, IAction };

export { getContextByName };
export default createContextProvider;
